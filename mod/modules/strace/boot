load_config

check_boot_dependencies busybox
status=$?

if [ $status -ne 0 ]; then
    log_msg "Enable busybox module in MODULES before this module"
else
    mount --bind ./files/strace /tmp/busybox/strace 2>> $MODULE_LOG_FILENAME
fi
